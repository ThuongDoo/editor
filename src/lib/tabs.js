// Sentinel activeId for the pinned "Giao diện" (theme) tab in SectionNav —
// not a real section id, so it can't collide with one. Shared between
// SchemaForm.jsx and SectionNav.jsx to avoid a circular import between them.
export const THEME_TAB_ID = '__theme__'
